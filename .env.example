# Production Database connection string
DATABASE_URL=do-not-put-this-here

# Development Database Configuration
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_HOST=localhost

# Phoenix Secrets (generate new ones with: mix phx.gen.secret)
SECRET_KEY_BASE=your_64_character_secret_key_base_here_replace_with_actual_secret
LIVE_VIEW_SIGNING_SALT=your_signing_salt_here_replace_with_actual_salt

# Discord webhook URL
DISCORD_WEBHOOK_URL=your_url

# Optional: Debug toggles
SHOW_DEBUG_PLAYER_VIEW=false
